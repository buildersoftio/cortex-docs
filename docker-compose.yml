services:
  # --- Prod profile: build-time Dockerfile above, served by Nginx on port 8080 ---
  cortex-docs:
    image: buildersoftdev/cortex-docs:v2.0.0
    ports:
      - "8080:80"
      - "8081:443"
    environment:
      NODE_ENV: production
    profiles: ["prod"]
