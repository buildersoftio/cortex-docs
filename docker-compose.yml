services:
  # --- Prod profile: build-time Dockerfile above, served by Nginx on port 8080 ---
  cortex-docs-prod:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
      - "8081:443"
    environment:
      NODE_ENV: production
    profiles: ["prod"]
